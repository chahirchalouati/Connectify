spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8888
    openfeign:
      circuitbreaker:
        alphanumeric-ids:
          enabled: true
  main:
    web-application-type: reactive
logging:
  level:
    reactor:
      netty: INFO
    org:
      springframework:
        cloud:
          gateway: TRACE