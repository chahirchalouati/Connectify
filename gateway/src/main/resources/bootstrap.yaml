spring:
  application:
    name: proxy-service
  cloud:
    config:
      uri: http://localhost:8888
  main:
    web-application-type: reactive
logging:
  level:
    org:
      springframework:
        web: INFO