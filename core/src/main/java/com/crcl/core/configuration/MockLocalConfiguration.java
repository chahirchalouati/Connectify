package com.crcl.core.configuration;

import org.springframework.context.annotation.Configuration;

/**
 * The MockLocalConfiguration class is a configuration class that is used in mocking local configurations.
 * It is annotated with the @Configuration annotation to indicate that it provides bean definitions and
 * other configuration options to the Spring application context.
 */
@Configuration
public class MockLocalConfiguration {
}
