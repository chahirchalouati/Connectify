server:
  port: 7004
# openApi documentation
api:
  title: Utilities Service
  description: API for Utilities services
---
spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      database: dev_utilities
      host: localhost
      port: 27017
      authentication-database: admin
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth-server:9000
          jwk-set-uri: http://auth-server:9000/oauth2/jwks
      client:
        registration:
          audit-service-oidc:
            provider: spring
            client-id: utilities-service
            client-secret: secret
            authorization-grant-type: client_credentials
            scope: openid
          audit-service:
            provider: spring
            client-id: utilities-service
            client-secret: secret
            authorization-grant-type: client_credentials
        provider:
          spring:
            issuer-uri: http://auth-server:9000
---
spring:
  config:
    activate:
      on-profile: docker
  data:
    mongodb:
      database: docker_utilities
      host: mongo
      port: 27017
      authentication-database: admin
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://authentication-service:9000
          jwk-set-uri: http://authentication-service:9000/oauth2/jwks
      client:
        registration:
          audit-service-oidc:
            provider: spring
            client-id: utilities-service
            client-secret: secret
            authorization-grant-type: client_credentials
            scope: openid
          audit-service:
            provider: spring
            client-id: utilities-service
            client-secret: secret
            authorization-grant-type: client_credentials
        provider:
          spring:
            issuer-uri: http://authentication-service:9000
---
spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      database: prod_utilities
      host: mongo
      port: 27017
      authentication-database: admin
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://authentication-service:9000
          jwk-set-uri: http://authentication-service:9000/oauth2/jwks
      client:
        registration:
          audit-service-oidc:
            provider: spring
            client-id: utilities-service
            client-secret: secret
            authorization-grant-type: client_credentials
            scope: openid
          audit-service:
            provider: spring
            client-id: utilities-service
            client-secret: secret
            authorization-grant-type: client_credentials
        provider:
          spring:
            issuer-uri: http://authentication-service:9000